from tkinter.font import names
from xml.dom.pulldom import CHARACTERS


names = ["Harry", "Ron", "Hermione"]

for name in names:
    print(name)  # it will print the names

for character in name:
    print(character)  # it will print the characters of the name line by line
